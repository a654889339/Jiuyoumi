<view class="page-address-edit">
  <view class="form-card card">
    <view class="form-item">
      <text class="form-label">联系人</text>
      <input class="form-input" placeholder="请输入联系人姓名" value="{{contactName}}" onInput="onNameInput" />
    </view>

    <view class="form-item">
      <text class="form-label">手机号</text>
      <input class="form-input" type="number" maxlength="11" placeholder="请输入手机号" value="{{contactPhone}}" onInput="onPhoneInput" />
    </view>

    <view class="form-item">
      <text class="form-label">国家/地区</text>
      <picker range="{{countries}}" value="{{countryIdx}}" onChange="onCountryChange">
        <view class="picker-value {{country ? '' : 'placeholder'}}">{{country || '请选择国家/地区'}}</view>
      </picker>
    </view>

    <view class="form-item" a:if="{{country === '其他'}}">
      <text class="form-label">自定义国家</text>
      <input class="form-input" placeholder="请输入国家/地区名称" value="{{customCountry}}" onInput="onCustomCountryInput" />
    </view>

    <view class="form-item" a:if="{{country === '中国大陆'}}">
      <text class="form-label">省市区</text>
      <input class="form-input" placeholder="请输入省 市 区，用空格分隔" value="{{regionText}}" onInput="onRegionInput" />
    </view>

    <view class="form-item">
      <text class="form-label">详细地址</text>
      <textarea class="form-textarea" placeholder="街道、楼栋、门牌号等" value="{{detail}}" onInput="onDetailInput" maxlength="200" auto-height="{{true}}" />
    </view>

    <view class="form-item switch-item">
      <text class="form-label">默认地址</text>
      <switch checked="{{isDefault}}" onChange="onDefaultChange" color="#667eea" />
    </view>
  </view>

  <view class="save-wrap">
    <button class="btn-save" onTap="save" disabled="{{saving}}">
      {{saving ? '保存中...' : '保存'}}
    </button>
  </view>
</view>
